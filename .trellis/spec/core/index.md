# Core 层

core 拥有 source identity/editor、scattering contract、runtime/material payload 与 canonical hash。公共合同不把任一 source 的原生图或参数归约为 LayerStack，也不把某个 backend state layout 提升为公共接口。

跨层签名、错误矩阵和测试要求见 `../project/unified-pipeline.md`。
